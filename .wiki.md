# Project Summary

“Atom 尝鲜”是一个面向产品想法表达与应用原型生成的 AI 工作台。用户通过自然语言描述需求，系统在浏览器中生成可运行的 HTML/CSS/JavaScript 小型应用，并支持需求迭代、交互预览、代码查看、测试、版本切换和 HTML 导出。

项目支持习惯打卡、待办、番茄专注及自定义记录类应用。生成应用通过 iframe 预览，并按项目隔离保存应用数据。项目状态同时保存在浏览器 `localStorage`；登录后可通过 Atoms Cloud 云端同步，实现跨设备恢复。

当前工作台新增本地“检查结果”标签和可恢复的模拟生成失败流程。检查结果基于本地规则分析当前版本，不上传数据；失败演示会保留当前预览、历史版本及用户数据，并提供重试和恢复上一版本操作。原有生成能力、三栏布局、项目管理、认证和云端同步保持不变。

# Project Module Description

- **项目创建页**
  - 支持自然语言需求、示例需求和 Ctrl/⌘ + Enter 快捷创建。
  - 输入少于 4 个字符时显示错误。
  - 展示最近项目，并支持打开和删除。
  - 根据认证状态显示本地保存、云端同步及登录状态。

- **项目状态与持久化**
  - 使用 `localStorage` 保存项目、UI 状态和应用数据。
  - 兼容旧项目数据，自动补全版本、CSS 和 JavaScript 字段。
  - 每个项目最多保留当前版本与上一版本。
  - 支持创建、需求修改、版本切换、刷新后恢复及云端同步。
  - 模拟生成失败不会清空当前版本或用户数据。

- **认证与用户体系**
  - 使用 Atoms Cloud OIDC + PKCE 登录流程。
  - 通过用户身份实现云端数据隔离。
  - 支持登录、退出、用户信息展示和本地与云端项目合并。

- **项目删除**
  - 左侧导航和创建页支持悬停删除。
  - 删除前提供行内二次确认。
  - 删除当前项目后自动切换到最近项目；全部删除后返回创建页。
  - 已同步项目会发起云端删除。

- **自然语言应用生成**
  - 基于本地规则识别习惯、待办、番茄专注和自定义应用类型。
  - 生成独立运行的 HTML、`styles.css` 和 `app.js`。
  - 生成过程提供构建状态，不依赖真实 AI 模型。
  - 支持需求追加并生成新版本。

- **需求沟通面板**
  - 展示初始需求、修改记录、创建时间和更新时间。
  - 追加需求后生成新版本，最多保留两个版本。
  - 生成期间显示状态并禁用相关操作。

- **左侧项目导航**
  - 提供产品标识、新建应用、当前项目、最近项目和设置入口。
  - 根据应用类型显示图标。
  - 展示认证状态、用户身份及云端同步状态。

- **预览工作区**
  - 提供预览、代码、测试、检查结果和日志标签。
  - 支持桌面端和手机端预览、刷新、全屏及 HTML 导出。
  - 支持三个生成文件切换、行号展示和代码复制。
  - 版本切换时同步更新预览、代码和检查结果。
  - 提供“模拟失败”演示入口；失败提示支持重试和恢复上一版本。

- **本地检查结果**
  - 使用 `checks.ts` 对当前版本执行四项规则检查：
    - 页面有可见内容。
    - 主要输入框或选择控件存在。
    - 主要按钮存在。
    - `localStorage` 已启用。
  - 每项显示通过或失败状态及简短说明。
  - 展示总体通过数量，检查仅在浏览器本地完成。

- **Atoms Cloud 后端**
  - 提供认证、用户隔离的数据访问和项目 CRUD API。
  - `atom_projects` 保存项目元数据、修改记录、版本 JSON 和客户端时间。
  - 支持数据库迁移、请求日志、耗时统计、健康检查及存储能力。

# Directory Tree

```text
app/
├── .mgx/config.yaml                    # 项目工具配置
├── start_app_v2.sh                     # 应用启动脚本
├── frontend/
│   ├── .mgx/config.yaml                # 前端工具配置
│   ├── README.md                       # 前端说明
│   ├── package.json                    # 依赖与脚本
│   ├── index.html                      # HTML 入口
│   ├── components.json                 # shadcn/ui 配置
│   ├── eslint.config.js                # ESLint 配置
│   ├── postcss.config.js               # PostCSS 配置
│   ├── tailwind.config.ts              # Tailwind 配置
│   ├── vite.config.ts                  # Vite 配置
│   ├── tsconfig*.json                  # TypeScript 配置
│   ├── site.config.json                # 站点配置
│   ├── template_config.json            # 模板配置
│   ├── requirements*.txt               # 前端运行环境依赖
│   ├── public/                         # 静态资源
│   └── src/
│       ├── App.tsx                     # 根组件
│       ├── App.css                     # 应用样式
│       ├── index.css                   # 全局主题与基础样式
│       ├── main.tsx                    # React 启动入口
│       ├── api/settings.ts             # 设置 API
│       ├── components/
│       │   ├── LoadingSpinner.tsx      # 加载状态组件
│       │   ├── ProtectedAdminRoute.tsx # 受保护路由
│       │   ├── blog/                   # 博客布局与 Markdown 渲染
│       │   ├── ui/                     # 通用 UI 组件
│       │   └── workbench/              # 工作台组件
│       ├── contexts/AuthContext.tsx    # 前端认证上下文
│       ├── hooks/
│       │   ├── useProjects.ts          # 项目、版本、失败状态与同步
│       │   ├── useHabits.ts            # 习惯状态与项目隔离存储
│       │   ├── use-mobile.tsx          # 移动端检测
│       │   └── use-toast.ts             # Toast 状态 Hook
│       ├── lib/
│       │   ├── api.ts                  # API 客户端
│       │   ├── appGenerator.ts         # 本地应用生成器
│       │   ├── auth.ts                 # 认证工具
│       │   ├── cloudSync.ts            # Atoms Cloud 项目与用户同步
│       │   ├── checks.ts               # 本地版本检查规则
│       │   ├── blog.ts                 # 博客数据工具
│       │   ├── config.ts               # 配置读取
│       │   └── utils.ts                # 通用工具函数
│       ├── pages/
│       │   ├── Index.tsx               # 创建页与三栏工作台入口
│       │   ├── AuthCallback.tsx        # 认证回调页
│       │   ├── AuthError.tsx           # 认证错误页
│       │   ├── LogoutCallbackPage.tsx  # 登出回调页
│       │   └── blog/                   # 博客页面
│       └── prerender/                  # 博客预渲染脚本
└── backend/
    ├── README.md                       # 后端说明
    ├── main.py                         # 后端应用入口
    ├── lambda_handler.py               # Serverless 入口
    ├── requirements*.txt               # Python 依赖
    ├── alembic.ini                     # 数据库迁移配置
    ├── alembic/                        # 数据库迁移文件
    ├── core/                           # 配置、认证、数据库与遥测
    ├── dependencies/                   # 依赖注入
    ├── middlewares/                    # 中间件
    ├── models/                         # ORM 数据模型
    ├── schemas/                        # API 数据结构
    ├── routers/                        # 后端路由
    ├── services/                       # 业务服务
    ├── skills_docs/                    # 云端能力说明
    ├── utils/                          # 后端工具
    └── data_models/
        └── atom_projects.json          # 项目表配置或数据模型
```

# File Description Inventory

| 文件或目录 | 说明 |
|---|---|
| `frontend/src/hooks/useProjects.ts` | 管理项目、版本、生成状态、模拟失败、重试、版本恢复、本地持久化和云端同步 |
| `frontend/src/lib/checks.ts` | 对当前 HTML 版本执行四项本地规则检查 |
| `frontend/src/components/workbench/PreviewWorkspace.tsx` | 实现预览、代码、测试、检查结果、日志、设备预览、失败恢复和导出 |
| `frontend/src/pages/Index.tsx` | 在创建页与编辑工作台之间切换，并连接失败恢复回调 |
| `frontend/src/lib/cloudSync.ts` | 封装用户查询、项目同步及云端 CRUD |
| `frontend/src/components/workbench/LeftNav.tsx` | 项目导航、认证状态、删除确认和登录/退出入口 |
| `frontend/src/components/workbench/CreatePage.tsx` | 创建页、示例需求、最近项目和认证入口 |
| `frontend/src/components/workbench/RequirementPanel.tsx` | 展示需求、修改记录、生成状态和结果提示 |
| `frontend/src/components/workbench/MiniApps.tsx` | 待办、番茄专注及其他小型应用 |
| `frontend/src/components/workbench/HabitApp.tsx` | 习惯打卡应用 |
| `frontend/src/hooks/useHabits.ts` | 习惯数据模型、项目隔离存储和日期状态处理 |
| `frontend/src/contexts/AuthContext.tsx` | 提供前端认证上下文 |
| `frontend/src/lib/auth.ts` | 认证辅助逻辑 |
| `backend/routers/atom_projects.py` | 云端项目数据接口与用户归属访问 |
| `backend/models/atom_projects.py` | 项目 ORM 模型 |
| `backend/services/atom_projects.py` | 项目业务服务 |
| `backend/routers/auth.py`、`backend/core/auth.py` | 登录认证、令牌解析及权限处理 |
| `backend/core/database.py`、`backend/dependencies/database.py` | 数据库连接与依赖注入 |
| `backend/routers/health.py` | 后端健康检查接口 |
| `backend/core/telemetry/` | 请求、数据库和执行耗时监控 |
| `frontend/src/components/ui/` | 可复用 UI 基础组件 |
| `frontend/src/components/blog/`、`frontend/src/pages/blog/` | 博客展示与文章渲染 |
| `frontend/src/main.tsx`、`frontend/src/App.tsx` | 前端启动入口和根组件 |
| `frontend/prerender/` | 博客路由、文章和站点地图预渲染辅助脚本 |

# Technology Stack

- 前端：React 18、TypeScript、Vite
- UI：Tailwind CSS、shadcn/ui 风格组件、Lucide React、Sonner
- 后端：Python、FastAPI 风格路由与服务层
- 数据层：SQLAlchemy、Alembic、Atoms Cloud 数据服务
- 认证：Atoms Cloud OIDC + PKCE、JWT 应用令牌、前端认证上下文
- SDK：`@metagptx/web-sdk`
- 本地存储：浏览器 `localStorage`
- 预览：iframe `srcDoc`、Sandbox
- 浏览器能力：Fullscreen API、Clipboard API、HTML Blob 导出
- 工程工具：ESLint、PostCSS、TypeScript
- 云端能力：项目 CRUD、用户隔离、状态同步、数据库迁移和请求遥测
- 应用生成与检查：本地规则生成器、本地静态检查，无真实 AI 模型依赖

# Usage

```bash
cd app/frontend
pnpm install
pnpm run lint
pnpm run build
pnpm run dev
```

后端依赖和数据库迁移可在 `app/backend` 目录按项目 README 配置和执行。

常用操作：

- `pnpm install`：安装前端依赖。
- `pnpm run lint`：执行 ESLint 检查。
- `pnpm run build`：构建生产版本。
- `pnpm run dev`：启动前端开发模式。
- 清空浏览器本地数据，验证空项目创建页。
- 输入习惯、待办、番茄或自定义记录需求，验证应用生成。
- 在工作台切换预览、代码、测试、检查结果和日志标签。
- 在“检查结果”中查看四项本地规则检查及通过数量。
- 在预览工具栏触发“模拟失败”，验证预览和数据保留。
- 使用“重试”清除失败状态，或在存在历史版本时恢复上一版本。
- 修改需求并生成新版本，验证版本切换、刷新持久化和云端同步。
- 在代码页切换文件、复制代码或导出 HTML。
- 使用桌面端和手机端预览验证响应式布局。
- 登录后验证用户隔离、云端拉取、合并、上传和删除。
- 运行 `pnpm run lint`、`pnpm run build` 及 TypeScript 类型检查验证代码。
