# Project Summary

“Atom 尝鲜”是一个纯前端 AI 应用原型创建与编辑工作台，面向产品想法表达、需求沟通、应用生成模拟、交互预览、代码查看与 HTML 导出。用户通过自然语言描述需求，系统基于本地规则生成可在浏览器独立运行的 HTML/CSS/JavaScript 小型应用，并支持编辑、测试、预览和版本对比。

项目支持待办、习惯打卡、番茄专注、记账、计算器、投票、倒计时及通用记录型应用。生成结果通过 iframe 渲染，并使用 `localStorage` 持久化。每个项目仅保留当前版本和上一版本，用户可切换版本，预览与代码内容会同步更新。当前仍是纯前端 MVP，不提供真实 AI 模型、后端服务、登录、支付或数据库。

项目加载时会清理历史开发验证产生的示例项目，避免污染最近项目列表。自然语言命名会保留“2028小程序”等数字与后缀组合，不再错误截断为纯数字。

# Project Module Description

- **项目创建页**
  - 支持自然语言需求输入、快捷键提交、示例需求和最近项目。
  - 输入少于 4 个字符时显示错误，不创建空项目。
  - 最近项目仅展示用户实际创建的项目。

- **项目管理与状态**
  - 使用 `localStorage` 保存项目列表、当前项目、页面模式、生成版本和应用数据。
  - 加载项目时自动清理历史开发示例数据。
  - 创建或重新生成时进入构建状态，约 2 秒后完成。
  - 生成期间保留旧预览，并叠加生成状态提示。
  - 每个项目最多保留两个版本，支持当前版本与上一版本切换。
  - 旧项目会自动迁移为 v1，并补全 CSS、JavaScript 等生成字段。
  - 项目标题会根据完整需求生成，保留数字与“小程序”等后缀。

- **自然语言应用生成**
  - 本地规则引擎识别应用标题、类型、字段、按钮和核心交互。
  - 生成结果拆分为完整 HTML、`styles.css` 内容和 `app.js` 内容。
  - 支持番茄专注、倒计时、记账、计算器、投票、待办、习惯及通用记录应用。
  - 生成应用可独立运行，并通过 `localStorage` 保存数据。
  - 涉及登录、支付、数据库、后端接口、地图、实时通信等能力时显示范围提示。

- **需求沟通面板**
  - 展示项目名称、初始需求、创建时间和修改记录。
  - 追加需求后写入修改记录，并触发新版本生成。
  - 生成期间显示状态并暂时禁用输入。
  - 完成后通过 Toast 提示生成成功或范围受限。

- **左侧项目导航**
  - 展示产品名称、新建应用、当前项目、最近项目和设置入口。
  - 根据应用类型显示对应图标，并支持项目切换。

- **预览工作区**
  - 提供预览、代码、测试和日志标签。
  - 通过 iframe 渲染生成的 HTML 应用。
  - 支持桌面端与手机端预览、刷新、全屏和 HTML 导出。
  - 代码页左侧提供 `index.html`、`styles.css`、`app.js` 文件切换。
  - 代码内容使用等宽字体和行号展示，支持复制当前文件代码。
  - 显示版本号、模板名称和生成时间，切换版本时预览与代码同步更新。
  - 测试面板模拟渲染、核心交互和本地持久化检查。

- **响应式交互**
  - 桌面端采用导航、需求面板和预览区三栏布局。
  - 小屏幕保留左侧导航，并通过顶部标签切换需求面板与预览区。
  - 预览区域支持移动设备外框，避免横向溢出。

# Directory Tree

```text
app/frontend/
├── .mgx/config.yaml                 # 项目工具配置
├── README.md                        # 前端项目说明
├── package.json                     # 依赖与脚本
├── index.html                       # HTML 入口
├── components.json                  # shadcn/ui 配置
├── eslint.config.js                 # ESLint 配置
├── postcss.config.js                # PostCSS 配置
├── tailwind.config.ts               # Tailwind 配置
├── vite.config.ts                   # Vite 配置
├── tsconfig*.json                   # TypeScript 配置
├── site.config.json                 # 站点配置
├── template_config.json             # 模板配置
├── prerender/                       # 博客预渲染脚本
├── public/                          # 静态资源
└── src/
    ├── App.tsx                      # 根组件
    ├── App.css                      # 应用样式
    ├── index.css                    # 全局主题与基础样式
    ├── main.tsx                     # React 启动入口
    ├── blog-routes.tsx              # 博客路由
    ├── components/
    │   ├── blog/                    # 博客布局与 Markdown 渲染
    │   ├── ui/                      # shadcn/ui 通用组件
    │   └── workbench/
    │       ├── CreatePage.tsx       # 无项目创建页
    │       ├── HabitApp.tsx         # 习惯应用
    │       ├── LeftNav.tsx          # 项目导航及类型图标
    │       ├── MiniApps.tsx         # 预置及通用小型应用
    │       ├── PreviewWorkspace.tsx # 预览、代码、测试、日志、版本与导出
    │       └── RequirementPanel.tsx # 需求沟通与生成状态
    ├── hooks/
    │   ├── useProjects.ts           # 项目、版本、生成流程、清理及持久化
    │   ├── useHabits.ts             # 习惯状态与项目隔离存储
    │   ├── use-mobile.tsx           # 移动端检测
    │   └── use-toast.ts             # Toast 状态 Hook
    ├── lib/
    │   ├── api.ts                   # API 工具
    │   ├── appGenerator.ts          # HTML/CSS/JavaScript 本地生成器
    │   ├── blog.ts                  # 博客数据工具
    │   ├── config.ts                # 配置读取
    │   └── utils.ts                 # 通用工具函数
    └── pages/
        ├── Index.tsx                # 创建页与编辑工作台入口
        ├── AuthCallback.tsx         # 认证回调页面
        ├── AuthError.tsx            # 认证错误页面
        └── blog/                    # 博客页面

uploads/
└── 202609020813593a9be0.png         # 项目上传资源
```

# File Description Inventory

| 文件或目录 | 说明 |
|---|---|
| `src/pages/Index.tsx` | 根据页面状态切换创建页与工作台，并将版本切换能力传入预览区 |
| `src/hooks/useProjects.ts` | 定义项目模型、双版本数据结构、生成流程、版本切换、旧数据迁移、示例数据清理、项目持久化和标题命名 |
| `src/lib/appGenerator.ts` | 根据自然语言需求生成完整 HTML，并拆分 CSS 与 JavaScript 内容 |
| `src/components/workbench/PreviewWorkspace.tsx` | 实现预览、代码文件切换、行号、复制、版本切换、测试、日志、设备预览、全屏和导出 |
| `src/components/workbench/CreatePage.tsx` | 创建页、示例需求、错误提示和最近项目入口 |
| `src/components/workbench/LeftNav.tsx` | 项目导航、最近项目及应用类型图标映射 |
| `src/components/workbench/RequirementPanel.tsx` | 初始需求、修改记录、需求输入、生成状态和结果提示 |
| `src/components/workbench/HabitApp.tsx` | 可交互的习惯打卡应用 |
| `src/components/workbench/MiniApps.tsx` | 待办、番茄专注及其他小型应用组件 |
| `src/hooks/useHabits.ts` | 习惯数据模型、项目隔离存储和跨日期状态重置 |
| `src/index.css` | 全局主题、颜色和基础渲染规则 |
| `src/components/ui/` | 可复用的 shadcn/ui 基础控件 |
| `src/components/blog/`、`src/pages/blog/` | 博客展示与文章渲染能力 |
| `src/main.tsx`、`src/App.tsx` | 前端启动入口和根组件挂载 |
| `prerender/` | 博客路由、文章和站点地图预渲染辅助脚本 |
| 根目录配置文件 | 管理依赖、构建、检查、TypeScript、Tailwind 和站点模板配置 |

# Technology Stack

- React 18
- TypeScript
- Vite
- Tailwind CSS
- shadcn/ui 风格组件
- Lucide React 图标
- Sonner Toast
- ESLint
- PostCSS
- 浏览器 `localStorage`
- iframe `srcDoc` 与 Sandbox
- 浏览器 Fullscreen API
- Clipboard API 与 HTML Blob 文件导出
- 无后端、无数据库、无真实 AI 服务

# Usage

```bash
cd app/frontend
pnpm install
pnpm run lint
pnpm run build
pnpm run dev
```

- `pnpm install`：安装前端依赖。
- `pnpm run lint`：执行 ESLint 检查。
- `pnpm run build`：构建生产版本。
- `pnpm run dev`：启动开发模式。
- 清空浏览器本地数据后重新进入，可验证无项目创建页。
- 刷新页面后，历史开发示例项目会被自动清理。
- 输入番茄、待办、记账、习惯、计算器、投票或倒计时需求，可验证对应应用生成。
- 输入“生成2028小程序”等数字加后缀需求，可验证项目标题保留完整名称。
- 输入其他记录型需求，可验证通用应用生成、交互和项目隔离持久化。
- 输入超出浏览器能力范围的需求，可验证范围提示。
- 在左侧导航或创建页最近项目区域切换项目。
- 在需求面板追加需求，等待新版本生成。
- 打开“代码”标签，在三个生成文件之间切换并查看行号。
- 在代码页复制当前文件内容，或导出当前版本 HTML。
- 在代码页切换当前版本与上一版本，验证预览和代码同步更新。
- 使用测试、日志标签查看检查结果和构建记录。
