# Project Summary

“Atom 尝鲜”是一个面向产品想法表达与应用原型生成的 AI 工作台。用户通过自然语言描述需求，系统在浏览器中生成可运行的 HTML/CSS/JavaScript 小型应用，并支持需求迭代、交互预览、代码查看、版本切换、失败恢复和 HTML 导出。

项目支持习惯打卡、待办、番茄专注、自定义记录及 2048 小游戏等应用。生成结果通过 iframe 隔离预览，项目、版本、界面状态和嵌入应用数据保存在浏览器 `localStorage`；登录后可通过 Atoms Cloud 同步，实现跨设备恢复。

当前生成器已支持识别包含“2048”的需求，并生成完整可玩的 2048 应用。创建页首个示例已替换为 2048 小游戏示例。前端 lint 与生产构建均已通过。

# Project Module Description

- **项目创建页**
  - 支持自然语言需求、示例需求和 Ctrl/⌘ + Enter 快捷创建。
  - 首个示例为支持键盘方向键和触屏滑动的 2048 小游戏。
  - 输入少于 4 个字符时显示错误。
  - 展示最近项目，并支持打开和删除。
  - 支持窄屏品牌文字截断及登录状态展示。

- **项目状态与持久化**
  - 使用 `localStorage` 保存项目、UI 状态和应用数据。
  - 兼容旧项目数据，自动补全版本、CSS 和 JavaScript 字段。
  - 每个项目最多保留当前版本与上一版本。
  - 支持创建、需求修改、版本切换、刷新恢复及云端同步。
  - 生成失败时保留旧版本预览和用户数据。

- **认证与用户体系**
  - 使用 Atoms Cloud OIDC + PKCE 登录流程。
  - 按用户隔离云端项目数据。
  - 支持登录、退出、用户信息展示、本地与云端项目合并。
  - 小屏导航隐藏头像，仅保留适配窄栏的退出入口。

- **项目删除**
  - 左侧导航和创建页支持悬停删除。
  - 删除前提供行内二次确认。
  - 删除当前项目后自动切换到最近项目；全部删除后返回创建页。
  - 已同步项目会发起云端删除。

- **自然语言应用生成**
  - 基于本地规则识别习惯、待办、番茄专注、自定义记录和 2048 应用类型。
  - 生成独立运行的 HTML、`styles.css` 和 `app.js`。
  - 2048 类型提供 4×4 棋盘、随机生成 2/4 方块、方向键操作、触屏滑动、合并计分、胜利提示和游戏结束处理。
  - 2048 最高分写入 `localStorage`，支持刷新保留；支持新游戏和再来一局。
  - 支持构建状态、追加需求和新版本生成，不依赖真实 AI 模型。

- **需求沟通面板**
  - 展示初始需求、修改记录、创建时间和更新时间。
  - 追加需求后生成新版本，最多保留两个版本。
  - 生成期间显示状态并禁用相关操作。

- **左侧项目导航**
  - 提供产品标识、新建应用、当前项目、最近项目及登录状态。
  - 支持项目选择、删除确认、登录和退出。
  - 桌面端显示完整导航，小屏收窄为图标栏。

- **预览工作区**
  - 提供预览、代码、检查结果和日志标签。
  - 支持桌面端和手机端预览、刷新、全屏及 HTML 导出。
  - 支持文件切换、行号展示和代码复制。
  - 支持版本切换并同步更新预览、代码和检查结果。
  - 提供模拟失败、重试和上一版本恢复。
  - 工具栏及标签区域适配窄屏，避免按钮重叠。
  - 2048 生成结果可在预览、代码、检查和导出功能中完整使用。

- **本地检查结果**
  - 使用 `checks.ts` 执行页面内容、输入控件、主要按钮和 `localStorage` 四项检查。
  - 展示各项通过状态、说明及总体通过数量。
  - 检查仅在浏览器本地完成。

- **Atoms Cloud 后端**
  - 提供认证、用户隔离的数据访问和项目 CRUD API。
  - `atom_projects` 保存项目元数据、修改记录、版本 JSON 和客户端时间。
  - 项目时间戳字段使用 `BIGINT`，兼容毫秒级时间戳。
  - 支持数据库迁移、请求日志、耗时统计和健康检查。

# Directory Tree

```text
app/
├── .mgx/config.yaml                    # 项目工具配置
├── start_app_v2.sh                     # 应用启动脚本
├── frontend/
│   ├── .mgx/config.yaml                # 前端工具配置
│   ├── README.md                       # 前端说明
│   ├── package.json                    # 依赖与脚本
│   ├── index.html                      # HTML 入口
│   ├── components.json                 # shadcn/ui 配置
│   ├── eslint.config.js                # ESLint 配置
│   ├── postcss.config.js               # PostCSS 配置
│   ├── tailwind.config.ts              # Tailwind 配置
│   ├── vite.config.ts                  # Vite 配置
│   ├── tsconfig*.json                  # TypeScript 配置
│   ├── site.config.json                # 站点配置
│   ├── template_config.json            # 模板配置
│   ├── requirements*.txt               # 前端运行环境依赖
│   ├── public/                         # 静态资源
│   └── src/
│       ├── App.tsx                     # 根组件
│       ├── App.css                     # 应用样式
│       ├── index.css                   # 全局主题与基础样式
│       ├── main.tsx                    # React 启动入口
│       ├── api/settings.ts             # 设置 API
│       ├── components/
│       │   ├── LoadingSpinner.tsx      # 加载状态组件
│       │   ├── ProtectedAdminRoute.tsx # 受保护路由
│       │   ├── blog/                   # 博客布局与 Markdown 渲染
│       │   ├── ui/                     # 通用 UI 组件
│       │   └── workbench/              # 工作台组件
│       ├── contexts/AuthContext.tsx    # 前端认证上下文
│       ├── hooks/
│       │   ├── useProjects.ts          # 项目、版本、失败状态与同步
│       │   ├── use-mobile.tsx          # 移动端检测
│       │   └── use-toast.ts             # Toast 状态 Hook
│       ├── lib/
│       │   ├── api.ts                  # API 客户端
│       │   ├── appGenerator.ts         # 本地应用生成器与应用类型分发
│       │   ├── auth.ts                 # 认证工具
│       │   ├── cloudSync.ts            # Atoms Cloud 项目同步
│       │   ├── checks.ts               # 本地版本检查规则
│       │   ├── blog.ts                 # 博客数据工具
│       │   ├── config.ts               # 配置读取
│       │   └── utils.ts                # 通用工具函数
│       ├── pages/
│       │   ├── Index.tsx               # 创建页与工作台入口
│       │   ├── AuthCallback.tsx        # 认证回调页
│       │   ├── AuthError.tsx           # 认证错误页
│       │   ├── LogoutCallbackPage.tsx  # 登出回调页
│       │   └── blog/                   # 博客页面
│       └── prerender/                  # 博客预渲染脚本
└── backend/
    ├── README.md                       # 后端说明
    ├── main.py                         # 后端应用入口
    ├── lambda_handler.py               # Serverless 入口
    ├── requirements*.txt               # Python 依赖
    ├── alembic.ini                     # 数据库迁移配置
    ├── alembic/
    │   └── versions/
    │       └── 8003505f50fe_auto_update.py # 时间戳字段类型迁移
    ├── core/                           # 配置、认证、数据库与遥测
    ├── dependencies/                   # 依赖注入
    ├── middlewares/                    # 中间件
    ├── models/                         # ORM 数据模型
    ├── schemas/                         # API 数据结构
    ├── routers/                        # 后端路由
    ├── services/                       # 业务服务
    ├── skills_docs/                    # 云端能力说明
    ├── utils/                          # 后端工具
    └── data_models/
        └── atom_projects.json          # 项目实体定义
```

# File Description Inventory

| 文件或目录 | 说明 |
|---|---|
| `frontend/src/components/workbench/CreatePage.tsx` | 创建页、2048 示例、最近项目和认证入口 |
| `frontend/src/components/workbench/LeftNav.tsx` | 项目导航、响应式图标栏、认证状态和删除确认 |
| `frontend/src/components/workbench/PreviewWorkspace.tsx` | 预览、代码、检查、日志、失败恢复和导出 |
| `frontend/src/components/workbench/RequirementPanel.tsx` | 需求、修改记录、生成状态和结果提示 |
| `frontend/src/hooks/useProjects.ts` | 项目、版本、生成状态、本地持久化和云端同步 |
| `frontend/src/lib/appGenerator.ts` | 本地应用类型识别与 HTML/CSS/JavaScript 生成，包含可玩的 2048 模板 |
| `frontend/src/lib/checks.ts` | 当前 HTML 版本的四项本地检查 |
| `frontend/src/lib/cloudSync.ts` | 用户查询、项目同步及云端 CRUD |
| `frontend/src/pages/Index.tsx` | 创建页与编辑工作台切换及状态连接 |
| `frontend/src/contexts/AuthContext.tsx` | 前端认证上下文 |
| `frontend/src/lib/auth.ts` | 认证辅助逻辑 |
| `backend/data_models/atom_projects.json` | `atom_projects` 实体字段与类型定义 |
| `backend/models/atom_projects.py` | 项目 ORM 模型；时间戳使用 `BigInteger`，保留 `id` 主键 |
| `backend/alembic/versions/8003505f50fe_auto_update.py` | 将三个时间戳字段从 `INTEGER` 升级为 `BIGINT` |
| `backend/routers/atom_projects.py` | 云端项目接口与用户归属访问 |
| `backend/services/atom_projects.py` | 项目业务服务 |
| `backend/routers/auth.py`、`backend/core/auth.py` | 登录认证、令牌解析及权限处理 |
| `backend/core/database.py`、`backend/dependencies/database.py` | 数据库连接与依赖注入 |
| `backend/routers/health.py` | 后端健康检查接口 |
| `backend/core/telemetry/` | 请求、数据库和执行耗时监控 |
| `frontend/src/components/ui/` | 可复用 UI 基础组件 |
| `frontend/src/components/blog/`、`frontend/src/pages/blog/` | 博客展示与文章渲染 |
| `frontend/src/main.tsx`、`frontend/src/App.tsx` | 前端启动入口和根组件 |
| `frontend/prerender/` | 博客路由、文章和站点地图预渲染辅助脚本 |

# Technology Stack

- 前端：React 18、TypeScript、Vite
- UI：Tailwind CSS、shadcn/ui 风格组件、Lucide React、Sonner
- 后端：Python、FastAPI 风格路由与服务层
- 数据层：SQLAlchemy、Alembic、Atoms Cloud 数据服务
- 数据库类型：项目客户端时间戳使用 PostgreSQL `BIGINT`
- 认证：Atoms Cloud OIDC + PKCE、JWT 应用令牌、前端认证上下文
- SDK：`@metagptx/web-sdk`
- 本地存储：浏览器 `localStorage`
- 预览：iframe `srcDoc`、Sandbox
- 浏览器能力：Fullscreen API、Clipboard API、HTML Blob 导出
- 工程工具：ESLint、PostCSS、TypeScript
- 云端能力：项目 CRUD、用户隔离、状态同步、数据库迁移和请求遥测
- 应用生成与检查：本地规则生成器、本地静态检查，无真实 AI 模型依赖

# Usage

```bash
cd app/frontend
pnpm install
pnpm run lint
pnpm run build
pnpm run dev
```

后端依赖和数据库迁移可在 `app/backend` 目录按项目 README 配置和执行。涉及 `atom_projects` 表结构变更时，应执行对应 Alembic 迁移，确保三个客户端时间戳字段为 `BIGINT`。

常用验证：

- 刷新页面，点击创建页首个 2048 示例。
- 验证 4×4 棋盘、方向键、触屏滑动、方块合并、计分和新游戏功能。
- 验证达到 2048 后的提示、无可移动时的结束覆盖层及再来一局。
- 刷新页面，确认最高分仍保留。
- 切换预览、代码、检查结果和日志标签。
- 修改需求并生成新版本，验证版本切换、刷新持久化和云端同步。
- 在代码页切换文件、复制代码或导出 HTML。
- 使用桌面端和窄屏设备验证工作台、工具栏和导航布局。
- 登录后验证用户隔离、云端拉取、合并、上传和删除。
- 执行前端 lint 与生产构建：

```bash
cd app/frontend
pnpm run lint
pnpm run build
```

- 执行后端语法检查：

```bash
cd app/backend
python -m py_compile models/atom_projects.py services/atom_projects.py routers/atom_projects.py
```

- 未登录访问项目实体接口时应返回认证错误，而不是数据库 500。
