# Project Summary

“Atom 尝鲜”是面向产品想法表达与应用原型生成的 AI 应用编辑器工作台。用户通过自然语言描述需求，系统生成可运行、可交互的 HTML/CSS/JavaScript 小应用，并支持预览、需求迭代、代码查看、版本切换、失败恢复、HTML 导出和云端同步。

项目内置 2048、待办、番茄专注、习惯打卡、倒计时、计算器、投票、记账及自定义记录等应用模板。生成结果通过 iframe 隔离预览，项目版本、界面状态及嵌入应用数据保存在浏览器 `localStorage`；登录后通过 Atoms Cloud 按用户同步。

项目支持在 Atoms 平台发布和分享应用。发布流程包括构建检查、App Viewer 预览、Publish 发布、发布链接编辑、Share 分享、权限设置及代码 Export 导出。

退出登录前会立即刷新待同步数据，仅清理已确认同步的账号项目及应用数据；同步失败或无云端副本的本地项目会保留，避免数据丢失。

# Project Module Description

- **创建页**
  - 支持自然语言需求、示例需求和 Ctrl/⌘ + Enter 创建。
  - 提供 2048 示例、最近项目、项目打开与删除。
  - 支持输入校验、登录状态展示及窄屏布局。

- **三栏工作台**
  - 左侧项目导航、中间需求沟通面板、右侧预览工作区。
  - 支持桌面端和移动端响应式布局。

- **项目状态与持久化**
  - 使用 `localStorage` 保存项目、UI 状态和应用数据。
  - 兼容旧项目数据并自动补全版本、CSS 和 JavaScript 字段。
  - 每个项目最多保留当前版本和上一版本。
  - 支持创建、需求修改、版本切换、刷新恢复及生成失败恢复。

- **自然语言应用生成**
  - 根据需求关键词识别应用类型并生成独立 HTML/CSS/JavaScript。
  - 支持 2048、待办、番茄、习惯、倒计时、计算器、投票、记账、通用记录及自定义应用。
  - 不依赖真实 AI 模型，使用本地规则生成器。

- **2048 应用**
  - 提供 4×4 棋盘、方向键、触屏滑动、合并计分、胜利及结束提示。
  - 支持移动过渡、合并弹跳、新方块弹出、按压反馈、震动反馈和最高分持久化。
  - 支持新游戏和再来一局。

- **需求与版本管理**
  - 展示初始需求、修改记录、创建时间和更新时间。
  - 追加需求后生成递增版本，支持版本切换并同步预览、代码和检查结果。

- **预览工作区**
  - 提供预览、代码、检查结果和日志标签。
  - 支持文件切换、行号、复制代码、刷新、全屏、设备切换和 HTML 导出。
  - 支持模拟生成失败、重试及恢复上一版本。

- **本地检查**
  - 检查页面内容、输入控件、主要按钮和 `localStorage`。
  - 展示单项结果及总体通过数量。

- **平台发布与分享**
  - 发布前执行本地构建检查。
  - 通过 App Viewer 预览后使用 Publish 发布。
  - 支持编辑发布链接、Share 分享、权限设置和 Export 导出代码。
  - 发布依赖平台 Cloud 能力，余额不足时需通过 Cloud & AI Wallet 充值。

- **认证与云端同步**
  - 使用 Atoms Cloud OIDC + PKCE 登录。
  - 按用户隔离云端项目，支持登录拉取、项目合并、上传、删除和退出清理。
  - OIDC 登录回调由平台提供，无需单独配置。
  - 退出前刷新待同步数据；同步失败的本地数据继续保留。

- **项目导航与删除**
  - 支持项目选择、新建、删除、登录和退出。
  - 删除前提供二次确认；删除当前项目后自动切换到最近项目。
  - 已同步项目同步发起云端删除。

- **Atoms Cloud 后端**
  - 提供认证、用户隔离、项目 CRUD、数据库迁移、健康检查和请求遥测。
  - 数据库、认证和对象存储由平台托管。
  - `atom_projects` 保存项目元数据、需求记录、版本 JSON 和客户端时间戳。

# Directory Tree

```text
app/
├── README.md                          # 项目说明、快速开始与部署说明
├── .mgx/config.yaml                   # 项目工具配置
├── start_app_v2.sh                    # 应用启动脚本
├── frontend/
│   ├── README.md                      # 前端说明
│   ├── package.json                   # 依赖与脚本
│   ├── index.html                     # HTML 入口
│   ├── components.json                # shadcn/ui 配置
│   ├── eslint.config.js               # ESLint 配置
│   ├── postcss.config.js              # PostCSS 配置
│   ├── tailwind.config.ts             # Tailwind 配置
│   ├── vite.config.ts                 # Vite 配置
│   ├── tsconfig*.json                 # TypeScript 配置
│   ├── site.config.json               # 站点配置
│   ├── template_config.json            # 模板配置
│   ├── requirements*.txt              # 前端运行环境依赖
│   ├── public/                        # 静态资源
│   └── src/
│       ├── App.tsx                    # 根组件与路由
│       ├── App.css                    # 应用样式
│       ├── index.css                  # 全局样式
│       ├── main.tsx                   # 前端启动入口
│       ├── api/settings.ts            # 设置 API
│       ├── components/
│       │   ├── LoadingSpinner.tsx     # 加载组件
│       │   ├── ProtectedAdminRoute.tsx# 受保护路由
│       │   ├── blog/                  # 博客布局与渲染
│       │   ├── ui/                    # 通用 UI 组件
│       │   └── workbench/             # 工作台组件
│       ├── contexts/AuthContext.tsx   # 认证上下文
│       ├── hooks/
│       │   ├── useProjects.ts         # 项目状态、持久化与同步
│       │   ├── use-mobile.tsx         # 移动端检测
│       │   └── use-toast.ts            # Toast 状态
│       ├── lib/
│       │   ├── api.ts                 # API 客户端
│       │   ├── appGenerator.ts        # 本地应用生成器
│       │   ├── auth.ts                # 认证工具
│       │   ├── cloudSync.ts           # 云端项目同步
│       │   ├── checks.ts              # 本地检查规则
│       │   ├── blog.ts                # 博客数据工具
│       │   ├── config.ts              # 配置读取
│       │   └── utils.ts               # 通用工具
│       ├── pages/
│       │   ├── Index.tsx              # 创建页与工作台入口
│       │   ├── AuthCallback.tsx       # 认证回调
│       │   ├── AuthError.tsx          # 认证错误页
│       │   ├── LogoutCallbackPage.tsx # 登出回调页
│       │   └── blog/                  # 博客页面
│       └── prerender/                 # 博客预渲染脚本
└── backend/
    ├── README.md                      # 后端说明
    ├── main.py                        # 后端入口
    ├── lambda_handler.py              # Serverless 入口
    ├── requirements*.txt              # Python 依赖
    ├── alembic.ini                    # 数据库迁移配置
    ├── alembic/versions/               # 迁移脚本
    ├── core/                          # 配置、认证、数据库与遥测
    ├── dependencies/                 # 依赖注入
    ├── middlewares/                  # 中间件
    ├── models/                       # ORM 模型
    ├── schemas/                      # API 数据结构
    ├── routers/                      # 后端路由
    ├── services/                     # 业务服务
    ├── skills_docs/                  # 云端能力说明
    ├── utils/                        # 后端工具
    └── data_models/
        └── atom_projects.json        # 项目实体定义
```

# File Description Inventory

| 文件或目录 | 说明 |
|---|---|
| `README.md` | 项目定位、功能、结构、数据存储、启动命令、使用流程和平台部署说明 |
| `frontend/src/components/workbench/CreatePage.tsx` | 创建页、示例需求、最近项目和认证入口 |
| `frontend/src/components/workbench/LeftNav.tsx` | 项目导航、响应式布局、认证和删除确认 |
| `frontend/src/components/workbench/RequirementPanel.tsx` | 需求记录、修改输入和生成状态 |
| `frontend/src/components/workbench/PreviewWorkspace.tsx` | 预览、代码、检查、日志、恢复和导出 |
| `frontend/src/hooks/useProjects.ts` | 项目、版本、本地持久化、云端同步及退出清理 |
| `frontend/src/lib/appGenerator.ts` | 应用类型识别及 HTML/CSS/JavaScript 生成 |
| `frontend/src/lib/checks.ts` | 当前版本的四项本地检查 |
| `frontend/src/lib/cloudSync.ts` | 云端用户查询、项目同步及 CRUD |
| `frontend/src/contexts/AuthContext.tsx` | 前端认证状态管理 |
| `frontend/src/lib/auth.ts` | 登录、退出和认证辅助逻辑 |
| `frontend/src/pages/Index.tsx` | 创建页与工作台切换 |
| `backend/data_models/atom_projects.json` | `atom_projects` 实体字段定义 |
| `backend/models/atom_projects.py` | 项目 ORM 模型及毫秒时间戳字段 |
| `backend/routers/atom_projects.py` | 云端项目接口与用户归属校验 |
| `backend/services/atom_projects.py` | 项目 CRUD 业务服务 |
| `backend/routers/auth.py`、`backend/core/auth.py` | 登录认证、令牌解析和权限处理 |
| `backend/core/database.py`、`backend/dependencies/database.py` | 数据库连接与依赖注入 |
| `backend/routers/health.py` | 后端健康检查接口 |
| `backend/core/telemetry/` | 请求、数据库和执行耗时监控 |
| `frontend/src/components/ui/` | 可复用 UI 基础组件 |
| `frontend/src/components/blog/`、`frontend/src/pages/blog/` | 博客展示与文章渲染 |
| `frontend/prerender/` | 博客路由、文章和站点地图预渲染 |

# Technology Stack

- 前端：React 18、TypeScript、Vite、React Router
- UI：Tailwind CSS、shadcn/ui 风格组件、Lucide React、Sonner
- 后端：Python、FastAPI 风格路由与服务层
- 数据层：SQLAlchemy、Alembic、PostgreSQL、Atoms Cloud
- 认证：Atoms Cloud OIDC + PKCE、JWT 应用令牌、认证上下文
- SDK：`@metagptx/web-sdk`
- 本地存储：浏览器 `localStorage`
- 预览与导出：iframe `srcDoc`、Sandbox、Fullscreen API、Clipboard API、HTML Blob
- 浏览器能力：Vibration API、触控事件
- 工程工具：ESLint、PostCSS、TypeScript
- 应用生成与检查：本地规则生成器、本地静态检查
- 平台部署：Atoms App Viewer、Publish、Share、Cloud & AI Wallet

# Usage

## 安装与本地开发

```bash
cd app/frontend
pnpm install
pnpm run dev
```

## 构建与检查

```bash
cd app/frontend
pnpm run lint
pnpm run build
```

构建产物为 `dist/`。将其作为静态文件托管时，前端登录、云端同步及相关后端能力不可用。

## 后端配置

后端依赖和数据库配置可在 `app/backend` 目录按项目 README 执行。涉及 `atom_projects` 表结构变更时，使用对应 Alembic 迁移；现有表迁移已就绪，客户端时间戳字段使用 `BIGINT`。

```bash
cd app/backend
python -m py_compile models/atom_projects.py services/atom_projects.py routers/atom_projects.py
```

## Atoms 平台部署

1. 完成本地 `lint` 和 `build` 检查。
2. 在平台 App Viewer 中确认构建产物和应用交互。
3. 使用 Publish 发布应用，并按需编辑发布链接。
4. 使用 Share 分享应用，配置访问权限或导出代码。
5. 发布依赖 Cloud 能力；Cloud 或 AI 余额不足时，根据页面提示进入 Cloud & AI Wallet 充值。
6. 数据库、认证、对象存储和 OIDC 登录回调由平台托管，无需额外配置。

## 功能验证

- 创建 2048 示例，验证方向键、触屏滑动、合并、计分、新游戏和最高分持久化。
- 切换预览、代码、检查结果和日志标签。
- 修改需求并生成新版本，验证双版本切换、刷新恢复和失败恢复。
- 在代码页切换文件、复制代码并导出 HTML。
- 使用桌面端和窄屏设备验证工作台、工具栏和导航布局。
- 登录后验证项目拉取、合并、上传、用户隔离和删除同步。
- 修改项目后立即退出，确认待同步数据先上传。
- 模拟同步失败，确认未成功同步的本地项目仍被保留。
