# Project Summary

“Atom 尝鲜”是一个纯前端 AI 应用原型创建与编辑工作台，面向产品想法表达、需求沟通、应用生成模拟、交互预览和导出。用户可用自然语言描述需求，系统通过本地规则生成可在浏览器独立运行的 HTML/CSS/JavaScript 小型应用，并在保持原有三栏布局与视觉风格的前提下进行编辑、测试和预览。

项目支持待办、习惯打卡、番茄专注、记账、计算器、投票、倒计时及通用记录型应用。生成结果通过 iframe 渲染，具备真实交互和 `localStorage` 持久化能力。当前仍是纯前端 MVP，不提供真实 AI 模型、后端服务、登录、支付或数据库；超出浏览器内运行范围的需求会显示明确提示。

# Project Module Description

- **项目创建页**
  - 支持自然语言需求输入、快捷键提交、示例需求和最近项目。
  - 输入过短时显示错误，不创建空项目。

- **项目管理与状态**
  - 使用 `localStorage` 保存项目列表、当前项目、页面模式、生成 HTML 和应用数据。
  - 项目创建或重新生成时进入构建状态，约 2 秒后完成。
  - 生成期间保留旧预览，不显示空白页面。
  - 支持新建应用、切换最近项目及返回创建页。
  - 兼容旧项目数据，缺失的生成字段会自动补全。

- **自然语言应用生成**
  - 本地通用规则引擎从需求中识别应用标题、类型、字段、按钮和核心交互。
  - 生成可独立运行的 HTML/CSS/JavaScript，并支持 HTML 快照导出。
  - 番茄专注支持 25 分钟倒计时、开始、暂停、重置和完成次数统计。
  - 倒计时支持时长设置、开始、暂停、重置和本地保存。
  - 记账支持收入、支出、备注、金额、余额统计和删除。
  - 计算器支持基础四则运算、清空、退格和结果计算。
  - 投票支持选项新增、投票、票数统计、进度展示和清空票数。
  - 待办、习惯和通用记录应用支持新增、完成、删除及持久化。
  - 涉及登录、支付、数据库、后端接口、地图、实时通信等能力时，生成浏览器内运行范围提示，不生成空白页或任务列表。

- **需求沟通面板**
  - 展示项目名称、初始需求、创建时间和修改记录。
  - 追加需求后立即写入修改记录，并触发应用重新生成。
  - 生成期间显示“正在生成”，输入框暂时禁用。
  - 完成后通过 Toast 提示生成成功或范围受限。

- **左侧项目导航**
  - 展示产品名称、新建应用、当前项目、最近项目和设置入口。
  - 根据应用类型显示对应导航图标。

- **预览工作区**
  - 支持预览、代码、测试和日志标签。
  - 通过 iframe 渲染真实生成的 HTML 应用。
  - 支持桌面端与手机端预览、刷新、全屏和 HTML 导出。
  - 代码标签展示实际生成的 HTML，导出功能下载可独立运行的文件。
  - 测试面板模拟渲染、核心交互和本地持久化检查。
  - 生成期间在现有预览上叠加状态提示。

- **响应式交互**
  - 桌面端采用导航、需求面板和预览区三栏布局。
  - 小屏幕保留左侧导航，并通过顶部标签切换需求面板与预览区。
  - 预览区域支持移动设备外框，避免横向溢出。

# Directory Tree

```text
app/frontend/
├── .mgx/config.yaml                 # 项目工具配置
├── README.md                        # 前端项目说明
├── package.json                     # 依赖与脚本
├── index.html                       # HTML 入口
├── components.json                  # shadcn/ui 配置
├── eslint.config.js                 # ESLint 配置
├── postcss.config.js                # PostCSS 配置
├── tailwind.config.ts               # Tailwind 配置
├── vite.config.ts                   # Vite 配置
├── tsconfig*.json                   # TypeScript 配置
├── site.config.json                 # 站点配置
├── template_config.json             # 模板配置
├── prerender/                       # 博客预渲染脚本
├── public/                          # 静态资源
└── src/
    ├── App.tsx                      # 根组件
    ├── App.css                      # 应用样式
    ├── index.css                    # 全局主题与基础样式
    ├── main.tsx                     # React 启动入口
    ├── blog-routes.tsx              # 博客路由
    ├── components/
    │   ├── blog/                    # 博客布局与 Markdown 渲染
    │   ├── ui/                      # shadcn/ui 通用组件
    │   └── workbench/
    │       ├── CreatePage.tsx       # 无项目创建页
    │       ├── HabitApp.tsx         # 习惯应用
    │       ├── LeftNav.tsx          # 项目导航及类型图标
    │       ├── MiniApps.tsx         # 预置及通用小型应用
    │       ├── PreviewWorkspace.tsx # 预览、代码、测试、日志与导出
    │       └── RequirementPanel.tsx # 需求沟通与生成状态
    ├── hooks/
    │   ├── useProjects.ts           # 项目模型、生成流程及持久化
    │   ├── useHabits.ts             # 习惯状态与项目隔离存储
    │   ├── use-mobile.tsx           # 移动端检测
    │   └── use-toast.ts             # Toast 状态 Hook
    ├── lib/
    │   ├── api.ts                   # API 工具
    │   ├── appGenerator.ts          # 本地自然语言应用生成器
    │   ├── blog.ts                  # 博客数据工具
    │   ├── config.ts                # 配置读取
    │   └── utils.ts                 # 通用工具函数
    └── pages/
        ├── Index.tsx                # 创建页与编辑工作台入口
        ├── AuthCallback.tsx         # 认证回调页面
        ├── AuthError.tsx            # 认证错误页面
        └── blog/                    # 博客页面

uploads/
└── 202609020813593a9be0.png         # 项目上传资源
```

# File Description Inventory

| 文件或目录 | 说明 |
|---|---|
| `src/pages/Index.tsx` | 根据页面状态切换创建页与三栏工作台，接入应用重新生成和生成中状态 |
| `src/hooks/useProjects.ts` | 定义项目模型、应用类型、需求解析、生成 HTML、构建状态、兼容旧数据及持久化 |
| `src/lib/appGenerator.ts` | 根据自然语言需求生成可运行的 HTML/CSS/JavaScript 小应用 |
| `src/components/workbench/CreatePage.tsx` | 创建页、示例需求、错误提示和最近项目入口 |
| `src/components/workbench/LeftNav.tsx` | 项目导航、最近项目及应用类型图标映射 |
| `src/components/workbench/RequirementPanel.tsx` | 初始需求、修改记录、需求输入、生成状态和结果提示 |
| `src/components/workbench/PreviewWorkspace.tsx` | iframe 应用预览、代码、测试、日志、设备切换、全屏和 HTML 导出 |
| `src/components/workbench/HabitApp.tsx` | 可交互的习惯打卡应用 |
| `src/components/workbench/MiniApps.tsx` | 待办、番茄专注及其他小型应用组件 |
| `src/hooks/useHabits.ts` | 习惯数据模型、项目隔离存储和跨日期状态重置 |
| `src/index.css` | 全局主题、颜色和基础渲染规则 |
| `src/components/ui/` | 可复用的 shadcn/ui 基础控件 |
| `src/components/blog/`、`src/pages/blog/` | 博客展示与文章渲染能力 |
| `src/main.tsx`、`src/App.tsx` | 前端启动入口和根组件挂载 |
| `prerender/` | 博客路由、文章和站点地图预渲染辅助脚本 |
| 根目录配置文件 | 管理依赖、构建、检查、TypeScript、Tailwind 和站点模板配置 |

# Technology Stack

- React 18
- TypeScript
- Vite
- Tailwind CSS
- shadcn/ui 风格组件
- Lucide React 图标
- Sonner Toast
- ESLint
- PostCSS
- 浏览器 `localStorage`
- iframe `srcDoc` 与 Sandbox
- 浏览器 Fullscreen API
- HTML Blob 文件导出
- 无后端、无数据库、无真实 AI 服务

# Usage

```bash
cd app/frontend
pnpm install
pnpm run lint
pnpm run build
pnpm run dev
```

- `pnpm install`：安装前端依赖。
- `pnpm run lint`：执行 ESLint 检查。
- `pnpm run build`：构建生产版本。
- `pnpm run dev`：启动开发模式。
- 清空浏览器本地数据后重新进入，可验证无项目创建页。
- 输入番茄、待办、记账、习惯、计算器、投票或倒计时需求，可验证对应应用生成。
- 输入其他记录型需求，可验证通用应用生成、交互和项目隔离持久化。
- 输入超出浏览器能力范围的需求，可验证范围提示。
- 在左侧导航或创建页最近项目区域切换项目。
- 在需求面板追加修改记录，等待约 2 秒完成重新生成。
- 使用代码、测试、日志标签查看工作区。
- 使用导出功能下载当前应用的 HTML 快照。
