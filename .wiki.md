# Project Summary

“Atom 尝鲜”是一个纯前端 AI 应用原型创建与编辑工作台，面向产品想法表达、需求沟通、应用生成模拟和交互预览。用户可以从自然语言需求创建项目，系统根据关键词匹配可运行的小型应用，并在编辑工作台中继续修改、预览、测试和导出。

项目区分“创建页”和“编辑页”两种状态：

- 没有项目时显示干净的创建页，不展示三栏工作台。
- 创建项目后进入三栏编辑工作台，展示需求、构建状态和对应应用。
- 项目、当前页面状态及应用数据均保存在浏览器 `localStorage`。
- 当前仍是纯前端 MVP，不实现真实 AI、后端服务、登录、支付或数据库。

# Project Module Description

- **项目创建页**
  - 展示 Atom 尝鲜品牌标识、主标题和应用描述输入框。
  - 支持开始创建、快捷键提交和 3 个示例需求填充。
  - 提供最近项目列表及无项目空状态。
  - 输入过短时停留在创建页并显示错误，不生成空项目。

- **项目管理与页面状态**
  - 使用 `localStorage` 保存项目列表、当前项目和页面模式。
  - 首次访问无项目时进入创建页。
  - 已有项目再次访问时进入最近打开的编辑页。
  - 支持从编辑页返回创建页，但不会清除已有项目。
  - 支持从最近项目或左侧导航重新打开项目。
  - 项目创建后经历短暂“构建中”状态，随后变为“已完成”。

- **需求沟通面板**
  - 展示当前项目、初始需求、创建时间和修改记录。
  - 支持追加修改需求，并以时间戳保存。
  - 使用 Toast 提供空输入、发送成功等反馈。

- **左侧项目导航**
  - 展示产品名称、新建应用、当前项目、最近项目和设置入口。
  - 项目列表使用真实持久化数据。
  - 新建应用仅返回创建页，不影响已有项目。

- **应用类型匹配**
  - 根据需求关键词在本地推断应用类型，不调用真实 AI。
  - 支持习惯打卡、待办清单和番茄专注三种应用。
  - 每个项目拥有独立的应用数据存储空间。

- **可运行小型应用**
  - **每日进步**：习惯新增、完成、取消完成、删除、进度统计和跨日期重置。
  - **待办清单**：待办新增、完成、取消完成、删除和进度统计。
  - **番茄专注**：25 分钟计时、开始、暂停、重置和完成次数统计。
  - 三种应用均支持按项目隔离的浏览器本地持久化。

- **预览工作区**
  - 支持预览、代码、测试和日志标签。
  - 支持桌面端与手机端预览、刷新、全屏和 HTML 快照导出。
  - 构建中显示加载反馈，不展示旧应用。
  - 测试面板模拟核心交互与持久化检查，并提供通过状态。
  - 日志面板展示项目创建、构建和需求修改记录。

- **响应式交互**
  - 桌面端保留导航、需求面板和预览区三栏布局。
  - 小屏幕保留左侧导航，并通过顶部标签切换需求面板和预览区。
  - 预览区域支持移动设备外框，避免页面横向溢出。

# Directory Tree

```text
app/frontend/
├── .mgx/config.yaml                 # 项目工具配置
├── README.md                        # 前端项目说明
├── package.json                     # 依赖与脚本
├── index.html                       # HTML 入口
├── components.json                  # shadcn/ui 配置
├── eslint.config.js                 # ESLint 配置
├── postcss.config.js                # PostCSS 配置
├── tailwind.config.ts               # Tailwind 配置
├── vite.config.ts                   # Vite 配置
├── tsconfig*.json                   # TypeScript 配置
├── site.config.json                 # 站点配置
├── template_config.json             # 模板配置
├── prerender/                       # 博客预渲染脚本
├── public/                          # favicon、robots 等静态资源
└── src/
    ├── App.tsx                      # 根组件
    ├── App.css                      # 应用样式
    ├── index.css                    # 全局主题与基础样式
    ├── main.tsx                     # React 启动入口
    ├── blog-routes.tsx              # 博客路由
    ├── components/
    │   ├── blog/                    # 博客布局与 Markdown 渲染
    │   ├── ui/                     # shadcn/ui 通用组件
    │   └── workbench/
    │       ├── CreatePage.tsx       # 无项目创建页
    │       ├── HabitApp.tsx         # 每日进步习惯应用
    │       ├── LeftNav.tsx          # 项目导航
    │       ├── MiniApps.tsx         # 待办清单与番茄专注应用
    │       ├── PreviewWorkspace.tsx # 预览、代码、测试与日志工作区
    │       └── RequirementPanel.tsx # 需求沟通面板
    ├── hooks/
    │   ├── useProjects.ts           # 项目与页面状态持久化
    │   ├── useHabits.ts             # 习惯状态与项目隔离存储
    │   ├── use-mobile.tsx           # 移动端检测
    │   └── use-toast.ts             # Toast 状态 Hook
    ├── lib/
    │   ├── api.ts                   # API 工具
    │   ├── blog.ts                  # 博客数据工具
    │   ├── config.ts                # 配置读取
    │   └── utils.ts                 # 通用工具函数
    └── pages/
        ├── Index.tsx                # 创建页与编辑工作台入口
        ├── AuthCallback.tsx         # 认证回调页面
        ├── AuthError.tsx            # 认证错误页面
        └── blog/                    # 博客首页与文章页

uploads/
└── 202609020813593a9be0.png         # 项目上传资源
```

# File Description Inventory

| 文件或目录 | 说明 |
|---|---|
| `src/pages/Index.tsx` | 根据页面状态切换创建页与三栏编辑工作台，处理项目选择、创建和移动端视图 |
| `src/hooks/useProjects.ts` | 定义项目模型、应用类型、需求推断、构建状态及项目/UI 状态持久化 |
| `src/components/workbench/CreatePage.tsx` | 无项目创建页、示例需求、错误提示和最近项目入口 |
| `src/components/workbench/LeftNav.tsx` | 项目导航、新建应用、当前项目、最近项目和设置入口 |
| `src/components/workbench/RequirementPanel.tsx` | 初始需求、修改记录和需求发送输入框 |
| `src/components/workbench/PreviewWorkspace.tsx` | 应用预览、代码、测试、日志、设备切换、全屏和导出 |
| `src/components/workbench/HabitApp.tsx` | 可交互的习惯打卡应用 |
| `src/components/workbench/MiniApps.tsx` | 待办清单和番茄专注应用 |
| `src/hooks/useHabits.ts` | 习惯数据模型、项目隔离存储和跨日期状态重置 |
| `src/index.css` | 全局主题、颜色和基础渲染规则 |
| `src/components/ui/` | 可复用的 shadcn/ui 基础控件 |
| `src/components/blog/`、`src/pages/blog/` | 模板附带的博客展示与文章渲染能力 |
| `src/main.tsx`、`src/App.tsx` | 前端启动入口和根组件挂载 |
| `prerender/` | 博客路由、文章和站点地图预渲染辅助脚本 |
| 根目录配置文件 | 管理依赖、构建、检查、TypeScript、Tailwind 和站点模板配置 |

# Technology Stack

- React 18
- TypeScript
- Vite
- Tailwind CSS
- shadcn/ui 风格组件
- Lucide React 图标
- Sonner Toast
- ESLint
- PostCSS
- 浏览器 `localStorage`
- 浏览器 Fullscreen API
- HTML Blob 文件导出
- 无后端、无数据库、无真实 AI 服务

# Usage

```bash
cd app/frontend
pnpm install
pnpm run lint
pnpm run build
pnpm run dev
```

- `pnpm install`：安装前端依赖。
- `pnpm run lint`：执行 ESLint 检查。
- `pnpm run build`：构建生产版本。
- `pnpm run dev`：启动开发模式。
- 清空浏览器本地数据后重新进入，可验证无项目创建页。
- 填写需求并点击“开始创建”后进入编辑工作台。
- 在左侧导航或创建页最近项目区域切换项目。
- 在需求面板追加修改记录。
- 在预览区体验习惯、待办或番茄应用。
- 使用代码、测试、日志标签查看对应工作区。
- 使用导出功能下载当前应用的 HTML 快照。
