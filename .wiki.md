# Project Summary

“Atom 尝鲜”是一个纯前端全屏 AI 应用编辑器工作台 MVP，面向应用原型创建与交互预览。当前聚焦成熟的软件工作台体验，不实现真实 AI、代码生成、后端服务、登录、支付或任务管理。

工作台默认打开“习惯打卡”项目，右侧嵌入可操作的“每日进步”应用，支持习惯新增、完成、取消完成、删除、进度统计与本地持久化。

# Project Module Description

- **工作台布局**
  - 左侧约 220px 项目导航。
  - 中间约 340px 需求沟通面板。
  - 右侧自适应预览工作区。
  - 首次打开直接展示完整工作台与示例应用。

- **左侧导航**
  - 展示产品名称“Atom 尝鲜”。
  - 支持新建应用、项目切换和设置入口。
  - 当前项目具有明显选中状态。
  - 小屏幕下收起为抽屉式导航。

- **需求面板**
  - 展示项目名称和“已完成”状态。
  - 展示用户需求与修改记录。
  - 底部固定多行输入框，支持发送需求并追加修改记录。
  - 使用 Toast 提供发送成功、空输入等反馈。

- **预览工作区**
  - 支持“预览”和“代码”标签切换。
  - 支持桌面端和手机端预览切换。
  - 支持刷新、全屏和 HTML 快照导出。
  - 所有主要工具操作均提供即时反馈。

- **每日进步应用**
  - 展示当前日期、完成数量和进度条。
  - 默认提供阅读、运动、喝水、早睡四条习惯，初始完成 2/4。
  - 支持新增、完成、取消完成和删除习惯。
  - 习惯数据保存到浏览器 `localStorage`，刷新后保留；跨日期时重置完成状态。
  - 完成全部习惯时显示成功提示。

- **响应式交互**
  - 小屏幕隐藏桌面左栏，通过菜单打开抽屉。
  - 需求面板与预览工作区可切换。
  - 页面避免横向溢出，适配桌面和移动预览。

# Directory Tree

```text
app/frontend/
├── .mgx/config.yaml                 # 项目工具配置
├── README.md                        # 前端项目说明
├── package.json                     # 依赖与脚本
├── index.html                       # HTML 入口
├── components.json                  # shadcn/ui 配置
├── eslint.config.js                 # ESLint 配置
├── postcss.config.js                # PostCSS 配置
├── tailwind.config.ts               # Tailwind 配置
├── vite.config.ts                   # Vite 配置
├── tsconfig*.json                   # TypeScript 配置
├── site.config.json                 # 站点配置
├── template_config.json             # 模板配置
├── prerender/                       # 博客预渲染脚本
│   ├── blog.js
│   ├── blog-routes.js
│   ├── blog-sitemap.js
│   └── utils.js
├── public/
│   ├── favicon.svg                  # 网站图标
│   └── robots.txt                   # 搜索引擎规则
└── src/
    ├── App.tsx                      # 应用根组件
    ├── App.css                      # 应用样式
    ├── index.css                    # 全局主题与基础样式
    ├── main.tsx                     # React 启动入口
    ├── blog-routes.tsx              # 博客路由
    ├── components/
    │   ├── blog/
    │   │   ├── BlogArticleLayout.tsx # 博客文章布局
    │   │   └── MarkdownArticle.tsx   # Markdown 文章渲染
    │   ├── ui/                      # shadcn/ui 通用组件库
    │   └── workbench/
    │       ├── HabitApp.tsx          # 每日进步习惯应用
    │       ├── LeftNav.tsx           # 左侧项目导航
    │       ├── PreviewWorkspace.tsx  # 预览与代码工作区
    │       └── RequirementPanel.tsx  # 需求沟通面板
    ├── hooks/
    │   ├── useHabits.ts              # 习惯状态与 localStorage 持久化
    │   ├── use-mobile.tsx            # 移动端检测
    │   └── use-toast.ts              # Toast 状态 Hook
    ├── lib/
    │   ├── api.ts                    # API 工具
    │   ├── blog.ts                   # 博客数据工具
    │   ├── config.ts                 # 配置读取
    │   └── utils.ts                  # 通用工具函数
    └── pages/
        ├── Index.tsx                 # 工作台主页
        ├── AuthCallback.tsx          # 认证回调页面
        ├── AuthError.tsx             # 认证错误页面
        └── blog/
            ├── BlogIndexPage.tsx     # 博客首页
            └── BlogPostPage.tsx      # 博客文章页

uploads/
└── 202609020813593a9be0.png         # 项目上传资源
```

# File Description Inventory

| 文件或目录 | 说明 |
|---|---|
| `src/pages/Index.tsx` | 组合导航、需求面板和预览工作区，处理项目选择及响应式视图切换 |
| `src/components/workbench/LeftNav.tsx` | 项目导航、新建应用、最近项目和设置入口 |
| `src/components/workbench/RequirementPanel.tsx` | 展示需求与修改记录，支持输入和发送 |
| `src/components/workbench/PreviewWorkspace.tsx` | 预览/代码切换、设备预览、刷新、全屏和导出 |
| `src/components/workbench/HabitApp.tsx` | 可交互的每日习惯打卡应用 |
| `src/hooks/useHabits.ts` | 习惯模型、默认数据、增删改状态和本地存储 |
| `src/index.css` | 浅色工作台主题、绿色主色、黄色提醒色和基础渲染规则 |
| `src/components/ui/` | 按需复用的 shadcn/ui 基础控件 |
| `src/lib/utils.ts` | className 合并等通用前端工具 |
| `src/main.tsx`、`src/App.tsx` | 前端启动与根组件挂载 |
| `src/pages/blog/`、`src/components/blog/` | 模板附带的博客页面和文章渲染能力 |
| `prerender/` | 博客路由、文章和站点地图预渲染辅助脚本 |
| `public/` | favicon 与爬虫规则等静态资源 |
| 根目录配置文件 | 管理依赖、构建、代码检查、TypeScript、Tailwind 和站点模板配置 |

# Technology Stack

- React 18
- TypeScript
- Vite
- Tailwind CSS
- shadcn/ui 风格组件
- Lucide React 图标
- Sonner Toast 反馈
- ESLint
- PostCSS
- 浏览器 `localStorage`
- 浏览器 Fullscreen API
- HTML Blob 文件导出
- 无后端、无数据库

# Usage

```bash
cd app/frontend
pnpm install
pnpm run lint
pnpm run build
pnpm run dev
```

- `pnpm install`：安装前端依赖。
- `pnpm run lint`：执行 ESLint 检查。
- `pnpm run build`：构建生产版本。
- `pnpm run dev`：启动开发模式。
- 首次进入即可看到完整工作台和“每日进步”示例应用。
- 在预览区可直接新增和管理习惯，数据会保存到当前浏览器。
- 通过“代码”标签查看示例代码片段。
- 通过“导出”下载当前习惯数据生成的 HTML 快照。
