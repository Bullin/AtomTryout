# Project Summary

“Atom 尝鲜”是一个面向产品想法表达与应用原型生成的 AI 工作台。用户通过自然语言描述需求，系统在浏览器中生成可运行的 HTML/CSS/JavaScript 小型应用，并支持需求迭代、交互预览、代码查看、测试、版本切换和 HTML 导出。

项目支持习惯打卡、待办、番茄专注及自定义记录类应用。生成应用通过 iframe 预览，并按项目隔离保存应用数据。项目状态同时保存在浏览器 `localStorage`；登录后可通过 Atoms Cloud 云端同步，实现跨设备恢复。

当前已支持项目删除、Atoms Cloud 登录、用户隔离、云端项目拉取与合并、自动创建和更新云端记录，以及完整的登录状态界面。未登录时仅使用本地存储；登录后按用户隔离云端数据。项目删除会同步移除已关联的云端记录。界面会明确展示“正在加载”“已登录”或“未登录”状态，并提供登录与退出入口。

# Project Module Description

- **项目创建页**
  - 支持自然语言需求、示例需求和 Ctrl/⌘ + Enter 快捷创建。
  - 输入少于 4 个字符时显示错误。
  - 展示真实最近项目，并支持打开和删除。
  - 根据认证状态显示加载中、已登录云端保存中或未登录仅本地保存。
  - 未登录时提供登录入口，已登录时显示用户身份和退出入口。

- **项目状态与持久化**
  - 使用 `localStorage` 保存项目、UI 状态和应用数据。
  - 兼容旧项目数据，并自动补全版本、CSS 和 JavaScript 字段。
  - 每个项目最多保留当前版本与上一版本。
  - 创建、需求修改、版本切换和打开项目会更新项目时间。
  - 登录后自动拉取云端项目，并按更新时间合并本地与云端数据。
  - 本地独有项目会在后续同步时自动上传。

- **认证与用户体系**
  - 使用 Atoms Cloud 提供的 OIDC + PKCE 登录流程，不自建密码注册体系。
  - 通过应用令牌和用户身份实现用户级数据归属。
  - 前端认证状态分为加载中、已登录和未登录，加载期间不误判为未登录。
  - 已登录时展示昵称或邮箱，并支持退出登录。
  - 退出后清除会话并保留本地项目，重新登录后可再次合并同步。

- **项目删除**
  - 左侧导航和创建页的项目行在悬停时显示删除按钮。
  - 删除前提供行内二次确认，避免误删。
  - 删除当前项目后自动切换到最近项目；全部删除后返回创建页。
  - 已同步项目会同时发起云端删除。

- **自然语言应用生成**
  - 基于本地规则识别习惯、待办、番茄专注和自定义应用类型。
  - 生成独立运行的 HTML、`styles.css` 和 `app.js`。
  - 对超出浏览器或当前生成器能力的需求提供范围提示。
  - 生成过程模拟约 2 秒构建状态，不使用真实 AI 模型。

- **需求沟通面板**
  - 展示初始需求、修改记录、创建时间和更新时间。
  - 追加需求后生成新版本，并保留最近两个版本。
  - 生成期间显示状态并禁用相关操作。

- **左侧项目导航**
  - 提供产品标识、新建应用、当前项目、最近项目和设置入口。
  - 根据应用类型显示图标。
  - 底部展示认证三态、用户身份、云端同步状态及登录/退出入口。

- **预览工作区**
  - 提供预览、代码、测试和日志标签。
  - 支持桌面端和手机端预览、刷新、全屏及 HTML 导出。
  - 支持三个生成文件切换、行号展示和代码复制。
  - 版本切换时同步更新预览与代码。

- **Atoms Cloud 后端**
  - 提供认证、用户隔离的数据访问和项目 CRUD API。
  - `atom_projects` 表保存项目元数据、修改记录、版本 JSON 和客户端时间。
  - 通过 `user_id` 实现项目归属与用户间数据隔离。
  - 支持数据库迁移、请求日志、耗时统计和存储等基础能力。
  - 提供健康检查、设置、用户、认证和存储相关路由。

# Directory Tree

```text
app/
├── .mgx/config.yaml                    # 项目工具配置
├── start_app_v2.sh                     # 应用启动脚本
├── frontend/
│   ├── .mgx/config.yaml                # 前端工具配置
│   ├── README.md                       # 前端说明
│   ├── package.json                    # 依赖与脚本
│   ├── index.html                      # HTML 入口
│   ├── components.json                 # shadcn/ui 配置
│   ├── eslint.config.js                # ESLint 配置
│   ├── postcss.config.js               # PostCSS 配置
│   ├── tailwind.config.ts              # Tailwind 配置
│   ├── vite.config.ts                  # Vite 配置
│   ├── tsconfig*.json                  # TypeScript 配置
│   ├── site.config.json                # 站点配置
│   ├── template_config.json            # 模板配置
│   ├── requirements*.txt               # 前端运行环境依赖
│   ├── public/                         # 静态资源
│   └── src/
│       ├── App.tsx                     # 根组件
│       ├── App.css                     # 应用样式
│       ├── index.css                   # 全局主题与基础样式
│       ├── main.tsx                    # React 启动入口
│       ├── api/settings.ts             # 设置 API
│       ├── components/
│       │   ├── LoadingSpinner.tsx      # 加载状态组件
│       │   ├── ProtectedAdminRoute.tsx # 受保护路由
│       │   ├── blog/                   # 博客布局与 Markdown 渲染
│       │   ├── ui/                     # 通用 UI 组件
│       │   └── workbench/              # 工作台组件
│       ├── contexts/AuthContext.tsx   # 前端认证上下文
│       ├── hooks/
│       │   ├── useProjects.ts          # 项目、版本、删除、认证与同步
│       │   ├── useHabits.ts            # 习惯状态与项目隔离存储
│       │   ├── use-mobile.tsx          # 移动端检测
│       │   └── use-toast.ts             # Toast 状态 Hook
│       ├── lib/
│       │   ├── api.ts                  # API 客户端
│       │   ├── appGenerator.ts         # 本地应用生成器
│       │   ├── auth.ts                 # 认证工具
│       │   ├── cloudSync.ts            # Atoms Cloud 项目与用户同步
│       │   ├── blog.ts                 # 博客数据工具
│       │   ├── config.ts               # 配置读取
│       │   └── utils.ts                # 通用工具函数
│       ├── pages/
│       │   ├── Index.tsx               # 创建页与工作台入口
│       │   ├── AuthCallback.tsx        # 认证回调页
│       │   ├── AuthError.tsx           # 认证错误页
│       │   ├── LogoutCallbackPage.tsx  # 登出回调页
│       │   └── blog/                   # 博客页面
│       └── prerender/                  # 博客预渲染脚本
└── backend/
    ├── README.md                       # 后端说明
    ├── main.py                         # 后端应用入口
    ├── lambda_handler.py               # Serverless 入口
    ├── requirements*.txt               # Python 依赖
    ├── alembic.ini                     # 数据库迁移配置
    ├── alembic/
    │   ├── env.py                      # 迁移环境
    │   ├── script.py.mako              # 迁移模板
    │   └── versions/                   # 数据库迁移版本
    ├── core/
    │   ├── auth.py                     # 认证核心逻辑
    │   ├── config.py                   # 后端配置
    │   ├── database.py                 # 数据库连接
    │   ├── enums.py                    # 枚举定义
    │   ├── mask_crypto.py              # 敏感数据处理
    │   └── telemetry/                  # HTTP、SQLAlchemy 与耗时遥测
    ├── dependencies/                   # 认证与数据库依赖注入
    ├── middlewares/                    # 中间件
    ├── models/                         # ORM 数据模型
    ├── schemas/                        # API 数据结构
    ├── routers/
    │   ├── aihub.py                    # AI 能力接口
    │   ├── atom_projects.py            # 项目 CRUD 接口
    │   ├── auth.py                     # 认证接口
    │   ├── health.py                   # 健康检查
    │   ├── settings.py                 # 设置接口
    │   ├── storage.py                  # 存储接口
    │   └── user.py                     # 用户接口
    ├── services/                       # 认证、项目、用户、存储等业务服务
    ├── skills_docs/                    # 云端能力使用说明
    ├── utils/                          # 后端工具函数
    └── data_models/
        └── atom_projects.json          # 项目表配置或数据模型
```

# File Description Inventory

| 文件或目录 | 说明 |
|---|---|
| `frontend/src/hooks/useProjects.ts` | 管理项目模型、版本、创建、删除、切换、本地持久化、认证状态、用户信息、登录和退出 |
| `frontend/src/lib/cloudSync.ts` | 封装用户查询、登录、退出、项目列表及项目创建、更新、删除 |
| `frontend/src/components/workbench/LeftNav.tsx` | 展示项目导航、认证三态、用户身份、删除确认和登录/退出入口 |
| `frontend/src/components/workbench/CreatePage.tsx` | 创建页、示例需求、最近项目、删除确认和认证状态入口 |
| `frontend/src/pages/Index.tsx` | 在创建页与编辑工作台间切换，并向页面组件传递用户与退出能力 |
| `frontend/src/lib/appGenerator.ts` | 根据自然语言需求生成 HTML、CSS、JavaScript 和应用类型 |
| `frontend/src/components/workbench/PreviewWorkspace.tsx` | 实现预览、代码、测试、日志、版本、设备预览、全屏和导出 |
| `frontend/src/components/workbench/RequirementPanel.tsx` | 展示需求、修改记录、生成状态和结果提示 |
| `frontend/src/components/workbench/MiniApps.tsx` | 待办、番茄专注及其他小型应用 |
| `frontend/src/components/workbench/HabitApp.tsx` | 习惯打卡应用 |
| `frontend/src/hooks/useHabits.ts` | 习惯数据模型、项目隔离存储和日期状态处理 |
| `frontend/src/contexts/AuthContext.tsx` | 提供前端认证上下文 |
| `frontend/src/lib/auth.ts` | 认证辅助逻辑 |
| `backend/routers/atom_projects.py` | 云端项目数据接口与用户归属访问 |
| `backend/models/atom_projects.py` | 项目 ORM 模型 |
| `backend/services/atom_projects.py` | 项目业务服务 |
| `backend/routers/auth.py`、`backend/core/auth.py` | 登录认证、令牌解析及权限处理 |
| `backend/core/database.py`、`backend/dependencies/database.py` | 数据库连接与依赖注入 |
| `backend/alembic/versions/51cc7331691e_auto_update.py` | 项目表数据库迁移 |
| `backend/routers/health.py` | 后端健康检查接口 |
| `backend/core/telemetry/` | 请求、数据库和执行耗时监控 |
| `frontend/src/components/ui/` | 可复用 UI 基础组件 |
| `frontend/src/components/blog/`、`frontend/src/pages/blog/` | 博客展示与文章渲染 |
| `frontend/src/main.tsx`、`frontend/src/App.tsx` | 前端启动入口和根组件 |
| `frontend/prerender/` | 博客路由、文章和站点地图预渲染辅助脚本 |

# Technology Stack

- 前端：React 18、TypeScript、Vite
- UI：Tailwind CSS、shadcn/ui 风格组件、Lucide React、Sonner
- 后端：Python、FastAPI 风格路由与服务层
- 数据层：SQLAlchemy、Alembic、Atoms Cloud 数据服务
- 认证：Atoms Cloud OIDC + PKCE、JWT 应用令牌、前端认证上下文
- SDK：`@metagptx/web-sdk`，提供用户查询与异步退出能力
- 本地存储：浏览器 `localStorage`
- 预览：iframe `srcDoc`、Sandbox
- 浏览器能力：Fullscreen API、Clipboard API、HTML Blob 导出
- 工程工具：ESLint、PostCSS、TypeScript
- 云端能力：项目 CRUD、用户隔离、状态同步、数据库迁移、请求遥测
- 应用生成：本地规则生成器，无真实 AI 模型依赖

# Usage

```bash
cd app/frontend
pnpm install
pnpm run lint
pnpm run build
pnpm run dev
```

后端依赖与迁移可在 `app/backend` 目录按项目 README 配置和执行。

- `pnpm install`：安装前端依赖。
- `pnpm run lint`：执行 ESLint 检查。
- `pnpm run build`：构建生产版本。
- `pnpm run dev`：启动前端开发模式。
- 清空浏览器本地数据后，可验证空项目创建页。
- 输入习惯、待办、番茄或自定义记录需求，验证对应应用生成。
- 在创建页或左侧导航悬停项目，点击删除并完成行内确认。
- 删除当前项目后验证自动切换和空列表回退。
- 未登录时验证“仅本地保存”状态和登录入口。
- 登录后验证用户身份展示、云端项目拉取、本地与云端合并及自动上传。
- 在加载期间验证“正在检查登录状态”提示不会误显示为未登录。
- 已登录时验证创建页和左侧导航均提供退出入口。
- 退出后验证会话清除、本地项目保留，以及重新登录后的再次同步。
- 修改需求、切换版本或创建项目后验证云端同步状态。
- 在代码页切换生成文件、复制代码或导出 HTML。
- 使用测试和日志标签查看渲染、交互及持久化检查结果。
